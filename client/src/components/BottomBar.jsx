const BottomBar = () => {
  return (
    <div className="mt-auto p-2 border-t border-gray-700">
      <select className="w-full bg-gray-800 text-xs p-1 rounded">
        <option>Mayank</option>
        <option>Settings</option>
        <option>Logout</option>
      </select>
    </div>
  );
};

export default BottomBar;